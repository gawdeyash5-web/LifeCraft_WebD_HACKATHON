import QuestPanel from './QuestPanel';

/**
 * Quests Feature Module (Owned by Member 2)
 * 
 * Backward-compatible entry point that wraps and re-exports the modular QuestPanel.
 * Satisfies the root layout integration contract without requiring edits to App.jsx.
 */
export default QuestPanel;
export { QuestPanel };
