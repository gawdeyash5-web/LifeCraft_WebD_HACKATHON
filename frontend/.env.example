# ============================================================
# LIFECRAFT Frontend Environment Variables (Vercel / Local Dev)
# ============================================================
# In Vercel full-stack production, API requests automatically route to /api on the same domain.
# VITE_API_URL is optional, and only needed if pointing to a custom external API.
# VITE_API_URL=https://api.yourdomain.com/api
